.claude/settings.local.json

# backend (Python)
backend/.venv/
backend/__pycache__/
backend/**/__pycache__/
backend/*.egg-info/
backend/.env
backend/.pytest_cache/

# apps Flutter: cada um já tem seu próprio .gitignore (gerado por
# `flutter create`), que cobre .dart_tool/, build/, etc. dentro da pasta.
